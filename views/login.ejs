<h1>
  로그인페이지
</h1>
<div>
  <form action="/login" method="post">
    <div>
      <% if (error) { %>
        <span><%= error %> </span><br>
      <% } %>
    </div>
    <div>
      <% if (message) { %>
        <span><%= message %> </span><br>
      <% } %>
    </div>
    <div>
      <label>이메일
        <input type="email" name="email" placeholder="Email" required />
      </label>
    </div>
    <div>
      <label>비밀번호
        <input type="password" name="password" placeholder="Password" required minlength="8" />
      </label>
    </div>
    <div>
      <button type="submit">로그인</button>
    </div>
    </div>
  </form>
</div>
<div>
  <form method="get">
    <div>
      <button formaction="/signup" type="submit">회원가입</button>
      <button formaction="/" type="submit">진행중인 투표 보러가기</button>
    </div>
  </form>
</div>
